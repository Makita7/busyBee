import useForecast from "../hooks/useForecast";

const ForecastInner = ({forecast}) => {
  // const { forecast } = useForecast();
  console.log(forecast);

  return (
    <div></div>
  )
}

export default ForecastInner